import { NullOnHideDirective } from './null-on-hide.directive';

describe('NullOnHideDirective', () => {
  it('should create an instance', () => {
    const directive = new NullOnHideDirective();
    expect(directive).toBeTruthy();
  });
});
